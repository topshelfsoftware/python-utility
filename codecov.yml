coverage:
  status:
    project:
      default:
        target: 100%
        threshold: 1%

comment:
  layout: "header, diff, changes"
  behavior: default
  require_changes: false
  require_base: no
  require_head: no
  sticky: true
